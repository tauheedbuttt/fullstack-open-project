import { View } from "react-native";
import Logout from "../../../components/Logout";

const RiderProfile = () => {
  return (
    <View>
      <Logout />
    </View>
  );
};

export default RiderProfile;
