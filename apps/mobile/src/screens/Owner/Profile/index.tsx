import { View } from "react-native";
import Logout from "../../../components/Logout";

const OwnerProfile = () => {
  return (
    <View>
      <Logout />
    </View>
  );
};

export default OwnerProfile;
