import Modal from "../../components/Modal";
import { modal } from "../../config/modal";

const MapViewModal = () => {
  return (
    <Modal modalKey={modal.mapView} title={"Map View"} className="!max-w-2xl">
      {/* Map Content */}
      <div>dasd</div>
    </Modal>
  );
};

export default MapViewModal;
